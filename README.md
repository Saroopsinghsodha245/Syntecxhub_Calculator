# Syntecxhub_Calculator
A simple command-line calculator built in Python that supports addition, subtraction, multiplication, and division. It accepts user input, validates data, handles divide-by-zero errors, and separates calculation logic into functions for easy testing. A menu-driven interface allows repeated calculations or exit.
